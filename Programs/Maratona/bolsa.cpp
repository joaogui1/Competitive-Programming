#include <cstdio>
#include <iostream>

using namespace std;

int dp[20010][1010], p[1010];
int main(){
   ios_base::sync_with_stdio(0);
   int n, c;
   fin >> n >> c;
   for(int i = 1; i <= n; ++i)   fin >> p[i];

   


   return 0;
}
