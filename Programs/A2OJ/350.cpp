#include <bits/stdc++.h>

using namespace std;

int main(){
  cin.tie(0);
  ios_base::sync_with_stdio(0);
  int n;
  cin >> n;
  if(n == 0)
    cout << "Don't be lazy, it takes only few minutes, you can do it.\n";
  if(n == 1)
    cout << "Good Job\n";
  if(n > 1)
    cout << "You Rocks Man\n";

}
