#include <bits/stdc++.h>

using namespace std;
typedef long long int ll;
typedef pair <int, int> pii;

int main(){
  ios_base::sync_with_stdio(0);
  string a, b;
  cin >> a >> b;
  if(a == b)  cout << a << "\n";
  else  cout << 1 << "\n";


  return 0;
}
