#include <bits/stdc++.h>

using namespace std;

int main(){
  cin.tie(0);
  ios_base::sync_with_stdio(0);
  string s;
  cin >> s;
  cout << s;
  for(int i = s.size() - 1; i > -1; --i)  cout << s[i];
  cout << "\n";


  return 0;
}
