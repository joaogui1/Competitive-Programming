#include <bits/stdc++.h>

using namespace std;

vector <int> v;

int main(){
  cin.tie(0);
  ios_base::sync_with_stdio(0);
  int n, k, beg, fim;
  cin >> n >> k;
  v.assign(n, 0);
  for(int i = 0; i < n; ++i)  cin >> v[i];



  return 0;
}
