#include <bits/stdc++.h>

using namespace std;

string g[4], s[4];

int main(){
  cin.tie(0);
  ios_base::sync_with_stdio(0);
  for(int i = 0; i < 4; ++i)  cin >> g[i];
  for(int i = 0; i < 4; ++i)  cin >> s[i];

  return 0;
}
